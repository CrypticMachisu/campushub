// Person 3: shows events the current "user" has signed up for
// (via storage.js). Stub for now.
export default function MyEvents() {
  return (
    <div className="container" style={{ paddingTop: "2rem" }}>
      <h1>My Events</h1>
    </div>
  );
}
